# Q712: When test evidence is noisy, which evaluator error should the system prefer?
**Stage:** evaluation · **Sources:** 1 · **Marked load-bearing by:** 0

## Camps

### False prefer negatives over
- **Answer:** Prefer false negatives over false positives: unnecessary revision may recover, while a false positive prematurely returns incorrect code
- **Taken by:** reflexion_decision_inversion (1)
- **Assumes:** the described data unit and shape are stable.

## Shared assumption
Every camp assumes that the system must make or embody a determinate choice about: when test evidence is noisy, which evaluator error should the system prefer.

## Split
UNANIMOUS (all agree)
